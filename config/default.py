"""
    Default values, to be used for all environments
    or overridden by individual environments.
    An example might be setting DEBUG = False in config/default.py
    and DEBUG = True in config/development.py.

    All other environment variables shall extend this default config through `import`
"""
DEBUG = True
